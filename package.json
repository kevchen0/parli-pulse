{
  "name": "parli-pulse",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build",
    "backtest": "node scripts/backtest-fields.ts && node scripts/backtest-points.ts && node scripts/backtest-teams.ts",
    "backtest:fields": "node scripts/backtest-fields.ts",
    "backtest:points": "node scripts/backtest-points.ts",
    "backtest:teams": "node scripts/backtest-teams.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "fetch": "node --env-file=.env scripts/fetch.ts",
    "load": "node --env-file=.env scripts/load.ts",
    "rollup": "node --env-file=.env scripts/rollup.ts",
    "compare": "node --env-file=.env scripts/compare-top.ts",
    "diagnose": "node --env-file=.env scripts/diagnose.ts",
    "diagnostics": "node --env-file=.env scripts/build-diagnostics.ts",
    "speaks": "node --env-file=.env scripts/speaks.ts",
    "rate": "node --env-file=.env scripts/rate.ts",
    "validate:rating": "node --env-file=.env scripts/validate-rating.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.23.1",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "fflate": "^0.8.3",
    "pg": "^8.23.0"
  }
}
