export const metadata = { title: 'Method — Parli Pulse' };

/**
 * A placeholder while the methodology is rewritten.
 *
 * The page keeps its heading, so a reader who clicks Method arrives somewhere
 * that is recognisably the page they asked for rather than a bare line of text.
 * The content is held back: the full version lives on the `dev` branch and
 * ships when it reads clearly, and a half-explanation of how a figure is
 * produced invites a reader to draw conclusions from the half.
 */
export default function MethodPage() {
  return (
    <main className="wrap prose">
      <h1>Method</h1>
      <p className="comingsoon">Coming soon!</p>
    </main>
  );
}
