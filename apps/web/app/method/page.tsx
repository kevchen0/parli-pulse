export const metadata = { title: 'Method — Parli Pulse' };

/**
 * A placeholder while the methodology is rewritten.
 *
 * The full version lives on the `dev` branch and ships when it reads clearly.
 * Nothing is kept here in the meantime: a half-explanation of how a figure is
 * produced invites a reader to draw conclusions from the half.
 *
 * The anchors other pages link into (#rating, #prior, #shrink) are gone with
 * the sections they pointed at. They resolve to the top of the page, which is
 * the whole page, so nothing is missed.
 */
export default function MethodPage() {
  return (
    <main className="wrap">
      <p className="comingsoon">Coming soon!</p>
    </main>
  );
}
